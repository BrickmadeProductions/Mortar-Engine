#pragma once

#include "Renderer.h"

namespace MortarCore
{

    void Renderer::Submit(const Ref<Shader> &shader, const Ref<Model> &model, const Ref<Transform> &transform)
    {
        //setup shader data here and load model to vertex array buffer
        //TODO
        //RenderAPI::DrawIndexed();
    }
}